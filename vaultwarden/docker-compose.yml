services:
  vaultwarden:
    image: vaultwarden/server:latest
    container_name: vaultwarden
    restart: unless-stopped
    environment:
      DOMAIN: "https://vw.l.vrdm.cc"
    volumes:
      - ./vw-data/:/data/
    networks:
      - npm-network

networks:
  npm-network:
    external: true